import type { MetisAction } from '../ai/metisTools'
import { getWorkshopCase } from './metisWorkshopCase'

export const executeMetisAction = async (action: MetisAction) => {
  // Placeholder para evitar errores de sintaxis
}
